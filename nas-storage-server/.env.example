# Server Configuration
PORT=3001

# API Key for authentication (generate with: openssl rand -base64 32)
NAS_API_KEY=your-secure-api-key-here

# Path where files will be stored on the NAS
STORAGE_PATH=/path/to/your/storage/clips

# Public URL where files are accessible (your Cloudflare Tunnel URL)
PUBLIC_URL=https://nxrthstore.sweber.dev

# Maximum file size in bytes (default: 5GB)
MAX_FILE_SIZE=5368709120

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,https://nxrthstack.sweber.dev

# HTTPS for direct Tailscale uploads (bypasses tailscale serve proxy)
# Generate certs: sudo tailscale cert --cert-file /opt/nas-storage-server/tls.crt --key-file /opt/nas-storage-server/tls.key <hostname>.ts.net
HTTPS_PORT=3443
TLS_CERT_PATH=/opt/nas-storage-server/tls.crt
TLS_KEY_PATH=/opt/nas-storage-server/tls.key

# Then add to Vercel: NAS_TAILSCALE_URL=https://<hostname>.ts.net:3443
